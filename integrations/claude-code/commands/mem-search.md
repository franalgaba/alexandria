---
description: Search Alexandria memory for relevant context
argument-hint: <query>
---

Search Alexandria memory using hybrid retrieval (lexical + semantic).

Use this before attempting something that might have failed before.

```bash
alex search "$ARGUMENTS" --limit 10
```
